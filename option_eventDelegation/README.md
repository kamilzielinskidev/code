How to run:
```bash
# node version 22.7.0
npm install
npm t
```

Assumptions:
- fp-ts modules are first-class citizens in the project.
- nodeName is always a string as in docs
- nodeName casing is not important as it is not used anywhere
- there is no separate BrowserNode for easier code working and reviewing