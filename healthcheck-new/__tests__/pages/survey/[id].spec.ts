describe("On Survey page", () => {
  describe("When there is no user in app store", () => {
    it.todo("should navigate to Home page");
  });
  describe("When there is a user in app store", () => {
    it.todo("should render room name");
    it.todo("should render back button");
    it.todo("should render rating component for each category");
    it.todo("should render submit button");
  });
});

describe("When use submits rate all categories", () => {
  describe("When user submits the survey", () => {
    it.todo("should send proper request to backend");
    it.todo("should redirect to room page");
  });
});
